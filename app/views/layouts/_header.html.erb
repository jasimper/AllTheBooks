<div class="header">
  <div class="nav">
  <% if current_user %>
  <%= link_to icon('home', 'Your Library'), root_path, title: 'Your Library'  %>
  <%= link_to icon('calendar', 'Release Calendar'), events_path, title: 'New Release Calendar' %>
  <%= link_to icon('sign-out', 'Sign Out'), '/logout', title: 'Sign Out' %>
  <% else %>
    <%= link_to icon('sign-in', 'Sign In'), '/login' %>
    <%= link_to icon('edit', 'Sign Up'), '/signup' %>
  <% end %>
  </div>
  <div class="searchForm">
    <%= render partial: 'books/search' if current_user %>
  </div>
</div>
